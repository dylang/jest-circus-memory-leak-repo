{
  "name": "jest-circus-memory-leak",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "jest": "^24.0.0",
    "jest-circus": "^24.0.0"
  },
  "scripts": {
    "test": "node --expose-gc ./node_modules/.bin/jest --logHeapUsage --runInBand funky"
  }
}
